// ═══════════════════════════════════════════════════════════════
//  index.js — точка входа
// ═══════════════════════════════════════════════════════════════
// Раньше здесь было ~7000 строк. Теперь код разбит на:
//   core.js    — общее состояние, БД, кэши, хелперы, app/io
//   routes.js  — все HTTP-роуты (Express)
//   sockets.js — вся логика Socket.IO
//
// Порядок require() важен: сначала routes/sockets навешивают
// обработчики на общий app/io из core.js, и только потом main()
// подгружает данные из БД и запускает server.listen(...) — как и
// раньше, просто в этом файле вместо самого низа старого index.js.
// ═══════════════════════════════════════════════════════════════

require('./routes');
require('./sockets');

const { main } = require('./core');

main().catch(e => { console.error('❌ Ошибка запуска:', e); process.exit(1); });